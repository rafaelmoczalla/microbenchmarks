FROM node:latest

ENV SPARK_MASTER_NAME ${SPARK_MASTER_NAME}
ENV SPARK_MASTER_PORT ${SPARK_MASTER_PORT}

COPY ${SPARK_APP_ARCHIVE_BASE_NAME}-${SPARK_APP_ARCHIVE_VERSION}.jar ${SPARK_APP_FOLDER}/${SPARK_APP_ARCHIVE_BASE_NAME}-${SPARK_APP_ARCHIVE_VERSION}.jar

COPY submit.sh /

CMD ["/bin/bash", "/submit.sh"]
